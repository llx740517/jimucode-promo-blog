# V030 图文发布稿

## 标题

Codex / Claude Code 项目规则模板怎么写：前端、后端、Python、全栈示例

## 前两段短文案

这条用一个脱敏演示目录，把前端、后端、Python、全栈四类项目的规则模板录成一套可复用流程。

这篇主要解决：规则文件写得太泛，比如“写高质量代码”，AI 很难稳定执行。看完你能：按“项目类型 -> 规则文件 -> 任务 -> 验证”的顺序录模板课。建议先收藏，操作时对照配图一步步核对。

## 备用标题

每次都要重复解释项目规范？先把 `AGENTS.md` 和 `CLAUDE.md` 模板录清楚
积木代码助手教程 V030：项目规则模板怎么录

## 完整正文备用

这条用一个脱敏演示目录，把前端、后端、Python、全栈四类项目的规则模板录成一套可复用流程。Codex 重点看 `AGENTS.md`，Claude Code 重点看 `CLAUDE.md` 和 `.claude/rules/`。视频里会展示如何写具体规则、如何让 AI 复述规则、如何看 diff 和测试结果，以及哪些内容必须打码。

这篇适合刚开始接触积木代码助手、Codex 或 Claude Code 的同学。不要只盯着一个按钮或一条命令，建议按图里的顺序看：先看当前问题，再看操作路径，最后确认结果有没有真正跑通。

常见卡点：
规则文件写得太泛，比如“写高质量代码”，AI 很难稳定执行
把所有规则堆在一个文件里，前端、后端、Python、测试、权限混在一起
不知道 Codex 和 Claude Code 的规则入口不同：Codex 重点看 `AGENTS.md`，Claude Code 重点看 `CLAUDE.md` 和 `.claude/rules/`
录屏时只展示模板文本，看不出模板如何绑定项目目录、任务和验证命令

看完这篇，你应该能做到：
按“项目类型 -> 规则文件 -> 任务 -> 验证”的顺序录模板课
给前端项目写出可展示的 UI、路由、状态、测试规则
给后端项目写出接口、鉴权、错误处理、迁移和测试规则
给 Python 项目写出环境、格式化、类型检查、测试和数据脱敏规则

我的建议是，第一次操作时不要一边改很多地方，一边猜原因。先把页面、终端输出、配置文件、日志记录这几块分开看，哪一步不一致，就从那一步往回查。

如果你也在配置或使用 AI 编程工具，可以先收藏这篇。后面遇到类似问题时，按这条路线重新核对一遍，通常能更快判断下一步该看哪里。

## 配图说明

首图用 `cover-flow-images/V030-cover-douyin.png`。
第二张用 `cover-flow-images/V030-flow.png`。
后面从 `ppt-images/slide-01.png` 到 `ppt-images/slide-08.png` 里选关键步骤图。
如果平台限制图片数量，优先保留：流程图、关键操作、常见错误、结果确认。

## 标签

#Codex #ClaudeCode #AI编程 #项目规则 #AGENTS.md #CLAUDE.md #模板课 #前端实战
