# V026 图文发布稿

## 标题

Claude Code 权限模式怎么选：permission mode、tools、allowedTools 讲清楚

## 前两段短文案

这条用一个脱敏演示目录讲 Claude Code 权限怎么选：什么时候用 `plan`，什么时候用 `--tools` 限制读和搜索，`--allowedTools` 和 `--disallowedTools` 分别解决什么问题，`.claude/settings.json` 里的 allow / ask / deny 怎么理解。

这篇主要解决：不知道 `plan`、`default`、`acceptEdits`、`auto` 和 `bypassPermissions` 什么时候用。看完你能：按一个清晰顺序理解本条视频的核心操作路线。建议先收藏，操作时对照配图一步步核对。

## 备用标题

Claude Code 别一上来放满权限，先看这几个工具限制
积木代码助手入门：Claude Code 权限安全第一步

## 完整正文备用

这条用一个脱敏演示目录讲 Claude Code 权限怎么选：什么时候用 `plan`，什么时候用 `--tools` 限制读和搜索，`--allowedTools` 和 `--disallowedTools` 分别解决什么问题，`.claude/settings.json` 里的 allow / ask / deny 怎么理解。重点是让新手先保守进入项目，看清权限提示，再用 diff 和测试确认结果。

这篇适合刚开始接触积木代码助手、Codex 或 Claude Code 的同学。不要只盯着一个按钮或一条命令，建议按图里的顺序看：先看当前问题，再看操作路径，最后确认结果有没有真正跑通。

常见卡点：
不知道 `plan`、`default`、`acceptEdits`、`auto` 和 `bypassPermissions` 什么时候用
看到权限提示就直接确认，没有看清工具名、命令和路径
把 `--allowedTools` 误认为工具白名单，忽略了官方资料里“限制工具范围应使用 `--tools`”的区别
项目里有 `.env`、secrets、Token，但没有写 `permissions.deny`

看完这篇，你应该能做到：
按一个清晰顺序理解本条视频的核心操作路线
知道关键页面、终端输出或配置位置应该看哪里
知道哪些信息发布前需要脱敏，哪些内容需要以录屏现场为准

我的建议是，第一次操作时不要一边改很多地方，一边猜原因。先把页面、终端输出、配置文件、日志记录这几块分开看，哪一步不一致，就从那一步往回查。

如果你也在配置或使用 AI 编程工具，可以先收藏这篇。后面遇到类似问题时，按这条路线重新核对一遍，通常能更快判断下一步该看哪里。

## 配图说明

首图用 `cover-flow-images/V026-cover-douyin.png`。
第二张用 `cover-flow-images/V026-flow.png`。
后面从 `ppt-images/slide-01.png` 到 `ppt-images/slide-08.png` 里选关键步骤图。
如果平台限制图片数量，优先保留：流程图、关键操作、常见错误、结果确认。

## 标签

#ClaudeCode #AI编程 #权限安全 #工具限制 #permission #mode #allowedTools #disallowedTools
